import { InvoicesWorkspace } from "../page"

// Módulo de Cierre — etapa "Final Bill" (estado to_final_bill) lista para facturar.
export default function ClosingPage() {
  return <InvoicesWorkspace scope="closing" />
}
